# 运行依赖（源码运行时用）
#
# 说明：
# - 基础录屏：只需要 ffmpeg（外部程序）+ pynput（键鼠事件）
# - “保存到剪映草稿”：需要剪映技能链路相关依赖（pyJianYingDraft + Windows 自动化/类型库）
#
# 仍需要系统已安装 ffmpeg，并能在命令行直接运行 `ffmpeg`

# 键鼠事件采集
pynput

# 保存到剪映草稿（在 scripts/vendor 中 vendored 了 pyJianYingDraft 源码，但仍需这些第三方依赖）
uiautomation
pymediainfo
comtypes
pywin32-ctypes
